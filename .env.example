# Optional — a random key is generated on first run if not set.
# Set this to keep users logged in across container restarts.
SECRET_KEY=

# Optional — can also be configured in the app's Settings page after first login.
# On Linux, replace host.docker.internal with 172.17.0.1
OLLAMA_URL=http://host.docker.internal:11434